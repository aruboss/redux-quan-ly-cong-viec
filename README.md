# redux-quan-ly-cong-viec


# copy arr ms: newArr = [...oldArr];


# copy object ms: newObject = {...oldObject};

#xóa 1 phtu trong arr: splice  arr.splice(index, 1); xóa trong mảng arr phần tử thứ index, xóa 1 tử